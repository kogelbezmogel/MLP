#ifndef __UTILS__
#define __UTILS__

#include <string>
#include <vector>
#include <map>

// #include "model.h"


std::string str_representation( std::vector<size_t> size_vec );

// std::map<std::string, float> gradient_report(Model& model, SimpleTensor input);




#endif // __UTILS__